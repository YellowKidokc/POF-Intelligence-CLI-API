# House style

Use a near-black `#060708` background, `#0c0f14` panels, hairline `#222a35` borders, and a `#d1a922` gold accent. Headings use Georgia. Section labels are uppercase with generous letter spacing. Paths are monospace. Use restrained, accessible severity badges. All CSS and assets must be embedded; external URLs are forbidden.
