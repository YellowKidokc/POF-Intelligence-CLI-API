# Station

Put work in `inbox/`. Run `scripts\run_dry.bat` to preview and `scripts\run.bat` to execute. Claimed work moves through `waiting/`; outputs and per-item receipts remain auditable. Edit `station.json` and `prompt.md` to define the job.
